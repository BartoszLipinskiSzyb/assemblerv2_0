reg.1 + reg.0 -> reg.0
reg.1 - reg.0 -> reg.0
reg.1 & reg.0 -> reg.0
reg.1 ^ reg.0 -> reg.0
reg.1 | reg.0 -> reg.0
! reg.0 -> reg.0
// bitshift
reg.0 >> -> reg.0

// error
// reg.0 -> reg.1, reg.2

io TEST = 42
TEST + reg.0 -> reg.1
reg.2 -> reg.3, TEST

2 + reg.0 -> reg.2

// error
// reg.0 reg.1 -> reg.2
                        // test comment
loop:
    go loop

TEST -> reg.0
reg.1 -> TEST iftrue go loop
if!- go loop
